import React from 'react';
import classNames from 'classnames';

const Column122 = ({ className, children }) => (
    <div className={classNames('col-1-2-2', className)}>{children}</div>
);

export default Column122;
